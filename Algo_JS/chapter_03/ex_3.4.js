const arr = [1, 4, 9, 7, 6];

const min = Math.min(...arr);
const max = Math.max(...arr);

console.log("L'élément minim du tableau est: " + min);
console.log("L'élément maximum du tableau est: " + max);
